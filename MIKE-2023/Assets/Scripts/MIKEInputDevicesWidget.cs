using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MIKEInputDevicesWidget : MIKEExpandingWidget
{

    //[SerializeField] private VerticalLayoutGroup group;

    //// Start is called before the first frame update
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    group.spacing = Random.Range(0, 1);
    //    // This makes sure that all of the input device widgets will resize correctly in the vertical layout group when they are expanded
    //    LayoutRebuilder.ForceRebuildLayoutImmediate((RectTransform)this.transform);
    //}
}
